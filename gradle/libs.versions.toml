[versions]
minecraft = "1.12"
feather_mappings = "1.12+build.28"
fabric_loader = "0.16.14"
loom = "1.11-SNAPSHOT"
ploceus = "1.11-SNAPSHOT"

[libraries]
minecraft = { module = "com.mojang:minecraft", version.ref = "minecraft" }
feather_mappings = { module = "net.ornithemc:feather", version.ref = "feather_mappings" }
fabric_loader = { module = "net.fabricmc:fabric-loader", version.ref = "fabric_loader" }

[plugins]
fabric_loom = { id = "fabric-loom", version.ref = "loom" }
ploceus = { id = "ploceus", version.ref = "ploceus" }
